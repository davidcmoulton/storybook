{%- import '../../../html.twig' as html -%}

{% spaceless %}

  <button data-behavior="behavioral-button" {{- html.attributes(attributes|default({})) }}>
    {{- html.content(text) -}}
  </button>

{% endspaceless %}
